# 替换产品 PNG

把透明 PNG 放在这个文件夹，并使用以下文件名：

- lipstick-red.png
- tube-blue.png
- tube-pink.png
- multi-balm.png
- precision-stick.png

可选独立阴影图：在产品文件名末尾加 `-shadow`，例如 `lipstick-red-shadow.png`。

建议所有图片使用相同画布尺寸（例如 1200 × 1800 px），产品保持同一基线，不要紧贴物体裁切。缺少 PNG 时页面会自动显示内置占位产品。
